#include <bits/stdc++.h>

using namespace std;

void solve() {
    long long n;
    cin >> n;
    cout << ((n - 1) >> 1) << endl;
}

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
        solve();
    }
    return 0;
}